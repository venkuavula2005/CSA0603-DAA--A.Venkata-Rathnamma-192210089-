#include<stdio.h>
#include<string.h>
int main(){
    char s1[20],s2[20];
    printf("enter the string s1: ");
    scanf("%s",&s1);
    int len=strlen(s1);
    for(int i=0;i<len;i++){
        s2[i]=s1[i];
    }
    s2[len]='\0';
    printf("copied string s1 to s2: %s\n",s2);
    return 0;
}
